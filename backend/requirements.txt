fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
asyncpg==0.29.0
alembic==1.13.2
pydantic==2.9.2
pydantic-settings==2.5.2
passlib[bcrypt]==1.7.4
# passlib 1.7.4's backend-detection is incompatible with bcrypt>=4.1 (ValueError
# "password cannot be longer than 72 bytes" al inicializar, incluso con
# contraseñas cortas) — fijar bcrypt en una versión compatible.
bcrypt==4.0.1
python-multipart==0.0.9

pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
